import { SchemaConfig } from './create-schema';
import { Schema } from 'prosemirror-model';
export declare const defaultSchemaConfig: SchemaConfig;
export declare const getSchemaBasedOnStage: (stage?: string) => Schema<any, any>;
export declare const defaultSchema: Schema;
