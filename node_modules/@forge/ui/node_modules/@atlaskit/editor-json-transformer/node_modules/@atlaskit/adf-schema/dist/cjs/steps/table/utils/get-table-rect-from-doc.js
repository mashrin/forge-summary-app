"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var prosemirror_tables_1 = require("prosemirror-tables");
/**
 * Util to get the table rect from the docs
 * @param doc
 * @param tablePos
 */
function getTableRectFromDoc(doc, tablePos) {
    var table = doc.nodeAt(tablePos);
    // Check for table existence
    if (!table || table.type.name !== 'table') {
        throw new Error("No table at position \"" + tablePos + "\".");
    }
    // Create transform base on the doc
    var map = prosemirror_tables_1.TableMap.get(table);
    var start = doc.resolve(tablePos).start(1);
    return {
        map: map,
        table: table,
        tableStart: start,
        // Default to zero
        bottom: 0,
        left: 0,
        right: 0,
        top: 0,
    };
}
exports.getTableRectFromDoc = getTableRectFromDoc;
//# sourceMappingURL=get-table-rect-from-doc.js.map