import { ForgeElement, BackendRuntimePayload, BackendRuntimeContext, RenderEffect } from './types';
export declare const render: (elem: ForgeElement<Record<string, any>>) => (payload: BackendRuntimePayload, runtimeContext?: BackendRuntimeContext | undefined) => Promise<RenderEffect[]>;
